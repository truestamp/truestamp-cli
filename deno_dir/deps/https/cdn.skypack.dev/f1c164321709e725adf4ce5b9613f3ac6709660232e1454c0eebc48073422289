import { Base } from "../base.d.ts";
declare type HealthStatus = {
    status: string;
    description?: string;
    checks?: Record<string, any>;
    links?: string[];
};
export declare class Health extends Base {
    getHealth(): Promise<HealthStatus>;
}
export {};
